---
phase: 04-mouse-reactive-gradient-background-with-breathing-animation
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - dls/tokens.css
autonomous: false
requirements: [D-07]
nyquist_role: gated-dls-change

must_haves:
  truths:
    - "A single --dur-breath motion token exists in dls/tokens.css :root, its exact value confirmed by a human before commit (D-07)"
    - "No dark-mode variant of --dur-breath exists (duration tokens are theme-agnostic)"
    - "No other token or colour value is changed — this is the ONE isolated DLS change of the phase"
  artifacts:
    - path: "dls/tokens.css"
      provides: "The --dur-breath motion duration token (breathing cadence for the mobile fallback)"
      contains: "--dur-breath"
  key_links:
    - from: "dls/tokens.css --dur-breath"
      to: "src/styles/app.css .gradient-bg--breathing (added in Plan 03)"
      via: "animation: gradient-breathe var(--dur-breath) ..."
      pattern: "--dur-breath"
---

<objective>
Add exactly ONE new motion duration token, `--dur-breath` (~7000ms), to
`dls/tokens.css`, immediately after `--dur-slower: 560ms`. This is the single
DLS change of the phase. It exists so the mobile "breathing" fallback (Plan 03)
can express its ~6-8s cadence as a token rather than a raw ms literal (Rule 0).

Covers: D-07.

**⚠ RULE 0 — DLS BREAKING CHANGE.** Adding a token is a breaking change under
CLAUDE.md RULE 0. It was approved in principle during discuss-phase (2026-07-15,
D-07), but the *exact final value* still requires an explicit human nod before it
is written and committed. This plan is therefore `autonomous: false` and gated by
a `checkpoint:human-verify` that surfaces the proposed value. Nothing depends on
the token's numeric value until it is confirmed: Plan 03's breathing CSS
references `var(--dur-breath)` by name, so the value can be finalised here first.

Purpose: Keep the one permitted DLS edit isolated, explicit, and human-confirmed.
Output: one new line in the `:root` motion block of `dls/tokens.css`.
</objective>

<execution_context>
@$HOME/.claude/spectrum-os/workflows/execute-plan.md
@$HOME/.claude/spectrum-os/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/04-mouse-reactive-gradient-background-with-breathing-animation-/04-CONTEXT.md
@.planning/phases/04-mouse-reactive-gradient-background-with-breathing-animation-/04-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Exact insertion site. Extracted from dls/tokens.css lines 88-99. Do not re-explore. -->

dls/tokens.css :root motion block (insert AFTER line 95, `--dur-slower: 560ms;`):
```css
  /* ---- Motion ----
     Durations, easings and named keyframe presets. Components read
     these so timing stays consistent. All motion collapses to nothing
     under prefers-reduced-motion (see components.css). */
  --dur-fast: 120ms;
  --dur: 200ms;
  --dur-slow: 320ms;
  --dur-slower: 560ms;       /* <-- insert --dur-breath immediately after this line */
  --ease-out: cubic-bezier(0.2, 0.8, 0.2, 1);
  --ease-in-out: cubic-bezier(0.65, 0, 0.35, 1);
  --ease-spring: cubic-bezier(0.34, 1.56, 0.64, 1); /* gentle overshoot */
```

The [data-theme="dark"] block (tokens.css lines 109-141) contains NO `--dur*` values
— confirming motion durations are theme-agnostic. Do NOT add a dark variant.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1 [T-04-03a]: Confirm the exact --dur-breath value (RULE 0 gate)</name>
  <files>(none — checkpoint only; the write happens in Task 2)</files>
  <what-built>
    Nothing is written yet. This checkpoint surfaces the proposed token value for a
    human nod BEFORE it is added to the DLS, because adding a token is a RULE 0
    breaking change (CLAUDE.md), pre-approved in principle only (CONTEXT.md D-07).
  </what-built>
  <action>Do not write anything. Present the proposed `--dur-breath` value below and wait for an explicit human confirmation of the exact value before Task 2 writes it (RULE 0 gate).</action>
  <how-to-verify>
    Proposed addition to `dls/tokens.css` (:root motion block, after `--dur-slower: 560ms;`):

      --dur-breath: 7000ms;      /* breathing cadence (gradient-bg mobile fallback); pre-approved 2026-07-15 */

    1. This is a single duration token. D-07 specifies a ~6-8s cadence; 7000ms sits mid-range.
    2. It is the ONLY DLS change in the whole phase. No colour token, no component, no dark variant.
    3. Confirm the exact value: keep 7000ms, or specify another value in the 6000-8000ms range.
  </how-to-verify>
  <resume-signal>Reply "approved" to write `--dur-breath: 7000ms;`, or give an alternative value (e.g. "use 6500ms") to write that instead.</resume-signal>
</task>

<task type="auto">
  <name>Task 2 [T-04-03b]: Add --dur-breath to dls/tokens.css</name>
  <files>dls/tokens.css</files>
  <read_first>
    - dls/tokens.css lines 88-141 (the :root motion block AND the [data-theme="dark"] block — confirm no --dur* lives in dark, so no dark variant is added)
    - CLAUDE.md RULE 0 (this is the one pre-approved DLS edit; do nothing beyond it)
    - .planning/phases/04-mouse-reactive-gradient-background-with-breathing-animation-/04-PATTERNS.md (dls/tokens.css section — exact insertion point and comment format)
  </read_first>
  <action>
Insert a single line into the `:root` motion block of `dls/tokens.css`, on the line
immediately AFTER `--dur-slower: 560ms;` (currently line 95), using the value confirmed
in the checkpoint (default 7000ms), following the existing token+comment format:

```css
  --dur-breath: 7000ms;      /* breathing cadence (gradient-bg mobile fallback); pre-approved 2026-07-15 */
```

Change NOTHING else in the file. Do NOT add a `[data-theme="dark"]` variant — the dark block
(lines 109-141) has no `--dur*` tokens; duration is theme-agnostic (D-07). Do NOT touch any
colour, spacing, shadow, or easing token. Do NOT edit `dls/components.css` or `dls/DLS-RULES.md`.
  </action>
  <verify>
    <automated>grep -n "\-\-dur-breath" dls/tokens.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "\-\-dur-breath" dls/tokens.css` returns exactly one match (D-07; the VALIDATION.md command for D-07).
    - The match sits in the `:root` block, on the line directly after `--dur-slower: 560ms;` (verify by reading the surrounding lines).
    - `grep -c "\-\-dur-breath" dls/tokens.css` returns `1` — no duplicate and no dark-theme variant (theme-agnostic).
    - `git diff --stat dls/tokens.css` shows a single-line insertion (`1 insertion`), and `git diff dls/tokens.css` shows ONLY the `--dur-breath` line added — no other token changed (the isolated-change guarantee).
    - `git diff --name-only` shows `dls/components.css` and `dls/DLS-RULES.md` are NOT modified.
  </acceptance_criteria>
  <done>
    `--dur-breath` (human-confirmed value) added as one line in the :root motion
    block of dls/tokens.css, no dark variant, no other DLS file or token touched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| design contract (DLS) integrity | Editing `dls/tokens.css` mutates the single source of truth for the whole app; an unreviewed or wrong edit propagates everywhere. |

No runtime trust boundary is crossed (no code executes from this change until Plan 03
references the token). No network, auth, input, crypto, or persistence.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | `dls/tokens.css` (DLS integrity, RULE 0) | mitigate | Change is gated by a blocking `checkpoint:human-verify` that surfaces the exact value before write; grep + `git diff` acceptance proves exactly one line added and no other token/DLS file touched. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH.md audit: none). No install task; no legitimacy checkpoint required. |

No HIGH-severity threats. ASVS L1: no applicable category (RESEARCH.md Security Domain).
</threat_model>

<verification>
- `grep -n "\-\-dur-breath" dls/tokens.css` — token present (D-07).
- `grep -c "\-\-dur-breath" dls/tokens.css` returns `1` — single occurrence, no dark variant.
- `git diff dls/tokens.css` — only the one line added; no other token changed.
- `git diff --name-only` — `dls/components.css` and `dls/DLS-RULES.md` unchanged.
</verification>

<success_criteria>
- One `--dur-breath` token exists in the `:root` motion block, its value confirmed by a human at the checkpoint.
- No dark-mode variant; no other token, DLS file, or colour value changed.
</success_criteria>

<output>
After completion, create `.planning/phases/04-mouse-reactive-gradient-background-with-breathing-animation-/04-02-SUMMARY.md`
</output>
